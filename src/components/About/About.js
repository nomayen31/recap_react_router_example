import React from 'react';
import Header from '../Header/Header';

const About = () => {
    return (
        <div>
            <Header></Header>
            this is the true real about main page
        </div>
    );
};

export default About;